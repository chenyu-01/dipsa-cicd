name: Version Branch Workflow

on: 
  push:
    branches:    
      - 'v[0-9].[0-9]'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run build
        run: |
          # Build steps go here!
          echo "Building branch ${{ github.ref }}..."
